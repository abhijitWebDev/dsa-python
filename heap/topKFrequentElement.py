# The parent node in a heap should have minimum value and that should be a complete binary tree
from collections import Counter
def findTopKElements(arr):






# Driver code
arr = [1, 1, 1, 1, 2, 2, 3, 3]
k = 2

# Result
result = findTopKElements(arr, k)